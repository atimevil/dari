# OpenAI API 키 (필수) — https://platform.openai.com/api-keys 에서 발급
# 실제 키는 .env.local 에 넣으세요 (이 파일은 예시).
OPENAI_API_KEY=sk-...

# 사용할 모델 (선택). 기본값: gpt-4o
# 비용 우선이면 gpt-4o-mini 로 변경 가능.
DARI_MODEL=gpt-4o
